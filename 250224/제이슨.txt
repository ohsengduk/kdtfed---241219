json = javascript object Notation=자바스크립트 객체 표기법

제이슨 형식으로 되어진 파일을 만들어서 해당 파일을 자바스크립트로 
가져온 다음 Dom을 제어해서 해당 데이터를 반복해서 삽입!!!

제이슨의 자료 형태를 어떻게 구분하고 생성해야할지 감이 안 온다.

데이터와 DB 개념은 다르다
자바스크립트의 객체 표기법

Api 데이터를 가져올 때는 패치 함수를 사용한다.

제이슨이 형태에서 객체 형태로 바꾸어 줬야지 한다.

메서드 체이닝 기법

const productInfo = "./db/json";

fetch(productInfo).then((response) => console.log(response)).then((date)=>console.log(date););

map, filtered  많이 쓰임이 있음

배열과 반복문 장악!!!!!! 해야한다. 
배열의 형태의 자료 구조가 필요하다.

이터러블한 객체 = 반복문을 사용할 수 있다.

crud : 정보 값에 아이디를 부여 해서 만들 수 있다.

.온점 표기법 [배열] : 메서드 
객체 안에 배열이 있는 것이다. 하나씩 빼서 현 시점의 시간 밀리초 아이디는 같을 수 없다.

forEach = 개별적으로 찾아와서 무엇을 하는 것!!! 새로운 배열의 형태로 다시금 만드는 것!!
그 배열안에는 아이디가 없으닌깐!!  찾아와서 아이디를 는다 
map 이라는 메서는 함수는 아이디 값을 찾아와서 다시금 반환을 한다.

메서드 함수를
map : 찾아서 실행하고 배열로 만들어줌
forEach : 찾아서 실행하고 끝 

객체 형태로 값을 반환하고 
객체 형태의 자료 구조를 반환하고 끝낸다.

전개 연산자 [서로 다른 + 객채를] = 연계하는 것!!!    .... item,

뽑아서 연계시켜 준다. ++ 증감 연산자

id : 
{   } 객체 형태의 값을 반환하라고 하는 구나 

배열이 가지고 있는 딥 러닝, 머신 러닝,

includes() 해당 값을 가지고 있다 가지고 있지 않다. 라는 함수 
배열의 필터 함수가 많이 쓰인다. 밸류값이
"change"  이벤트 함수 바꾼다.

쏘트 중요함.

실제로 사용가능하다.
3시 55분 : 채널 톡 가입하기
https://channel.io/ko

딥러닝, 머싱러닝,
LMN

https://desk.channel.io/#/channels/193345/settings/general/plugins/194544

윈도우 객체 리로드하면 새로운 객체를 만든다.


    li.addEventListener("click", () => {
      window.location.href = "produ.html";
    })

페이지 라우팅을 했다라고 함. = 페이지를 분기했다.= 페이지를 만들어서 쪼개어 관리했다.
상품에 상품 명과 값을 줄 수 있다.

